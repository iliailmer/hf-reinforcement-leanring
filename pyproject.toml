[project]
name = "hf-reinforcement-leanring"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium>=1.1.1",
    "stable-baselines3>=2.6.0",
    "transformers>=4.52.3",
]

[tool.pyright]
venv=".venv"
venvPath="."
